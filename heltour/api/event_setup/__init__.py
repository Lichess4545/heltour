"""Setting up an event.

Creating leagues / seasons / sections, configuring schedules, deciding
the rating type, picking the scoring system, etc. This domain is fully
independent from running the event — code here should never depend on
``round_management`` or ``standings``.
"""
